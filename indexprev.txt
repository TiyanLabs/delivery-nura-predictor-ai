<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Delivery ETA Console</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;700&family=JetBrains+Mono:wght@400;500;700&display=swap" rel="stylesheet">
<style>
  :root {
    --ink: #10141A;
    --panel: #171D25;
    --panel-line: #262E3A;
    --paper: #E9EDF2;
    --paper-dim: #8992A3;
    --signal: #E8B93B;      /* dispatch-board amber */
    --signal-dim: #6b5a26;
    --route: #4FD1A5;       /* route-line green for the result */
  }

  * { box-sizing: border-box; }

  body {
    margin: 0;
    background: var(--ink);
    background-image:
      linear-gradient(var(--panel-line) 1px, transparent 1px),
      linear-gradient(90deg, var(--panel-line) 1px, transparent 1px);
    background-size: 42px 42px;
    background-position: -1px -1px;
    color: var(--paper);
    font-family: 'Space Grotesk', sans-serif;
    min-height: 100vh;
    padding: 40px 20px 80px;
  }

  .wrap { max-width: 980px; margin: 0 auto; }

  header {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    border-bottom: 1px solid var(--panel-line);
    padding-bottom: 18px;
    margin-bottom: 34px;
    flex-wrap: wrap;
    gap: 10px;
  }

  .brand { display: flex; align-items: baseline; gap: 10px; }
  .brand .mark {
    font-family: 'JetBrains Mono', monospace;
    color: var(--signal);
    font-size: 13px;
    letter-spacing: 0.14em;
    border: 1px solid var(--signal-dim);
    padding: 3px 8px;
    border-radius: 3px;
  }
  header h1 { font-size: 20px; font-weight: 500; margin: 0; }
  header .sub {
    font-family: 'JetBrains Mono', monospace;
    font-size: 12px;
    color: var(--paper-dim);
  }

  .grid {
    display: grid;
    grid-template-columns: 1.15fr 0.85fr;
    gap: 24px;
  }
  @media (max-width: 760px) { .grid { grid-template-columns: 1fr; } }

  .panel {
    background: var(--panel);
    border: 1px solid var(--panel-line);
    border-radius: 10px;
    padding: 26px;
  }

  .panel h2 {
    font-size: 12px;
    font-family: 'JetBrains Mono', monospace;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--paper-dim);
    margin: 0 0 20px;
  }

  .field-row { display: grid; grid-template-columns: 1fr 1fr; gap: 14px; }
  .field { margin-bottom: 16px; }
  .field label {
    display: block;
    font-size: 12px;
    color: var(--paper-dim);
    margin-bottom: 6px;
    font-family: 'JetBrains Mono', monospace;
  }
  .field input, .field select {
    width: 100%;
    background: var(--ink);
    border: 1px solid var(--panel-line);
    color: var(--paper);
    padding: 10px 11px;
    border-radius: 6px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 13.5px;
  }
  .field input:focus, .field select:focus {
    outline: none;
    border-color: var(--signal);
  }

  button {
    width: 100%;
    background: var(--signal);
    color: #1a1405;
    border: none;
    padding: 13px;
    border-radius: 6px;
    font-family: 'JetBrains Mono', monospace;
    font-weight: 700;
    font-size: 13px;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    cursor: pointer;
    margin-top: 6px;
  }
  button:hover { filter: brightness(1.08); }

  .result-panel {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    min-height: 100%;
  }
  .result-panel h2 { align-self: flex-start; }

  .eta-value {
    font-family: 'JetBrains Mono', monospace;
    font-size: 64px;
    font-weight: 700;
    color: var(--route);
    line-height: 1;
    margin: 10px 0 4px;
  }
  .eta-unit {
    font-family: 'JetBrains Mono', monospace;
    color: var(--paper-dim);
    font-size: 13px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .placeholder {
    color: var(--paper-dim);
    font-family: 'JetBrains Mono', monospace;
    font-size: 13px;
    line-height: 1.7;
  }

  .route-line {
    width: 100%;
    height: 2px;
    margin: 26px 0 22px;
    background: repeating-linear-gradient(
      90deg, var(--route), var(--route) 6px, transparent 6px, transparent 12px
    );
    opacity: 0.6;
  }

  .error {
    font-family: 'JetBrains Mono', monospace;
    font-size: 12.5px;
    color: #E8703B;
    border: 1px solid #4a2b1c;
    background: #201410;
    padding: 10px 12px;
    border-radius: 6px;
    margin-bottom: 16px;
  }

  footer {
    max-width: 980px;
    margin: 40px auto 0;
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    color: var(--paper-dim);
    text-align: center;
  }
</style>
</head>
<body>
<div class="wrap">

  <header>
    <div class="brand">
      <span class="mark">ETA·NET</span>
      <h1>Delivery Time Console</h1>
    </div>
    <div class="sub">CS361 · neural net regression</div>
  </header>

  <div class="grid">

    <div class="panel">
      <h2>Order details</h2>
      <form method="POST">
        <div class="field-row">
          <div class="field">
            <label>Market ID</label>
            <input type="number" step="1" name="market_id" value="{{ request.form.get('market_id', 2) }}" required>
          </div>
          <div class="field">
            <label>Order protocol</label>
            <input type="number" step="1" name="order_protocol" value="{{ request.form.get('order_protocol', 1) }}" required>
          </div>
        </div>

        <div class="field">
    <label>Store category</label>
    <select name="store_category">
      {% for cat in categories %}
      <option value="{{ cat }}" {% if request.form.get('store_category') == cat %}selected{% endif %}>{{ cat }}</option>
      {% endfor %}
    </select>
  </div>

        <div class="field">
          <label>Order placed at</label>
          <input type="datetime-local" name="order_time" value="{{ request.form.get('order_time', '') }}" step="60" required>
        </div>

        <div class="field-row">
          <div class="field">
            <label>Total items</label>
            <input type="number" step="1" name="total_items" value="{{ request.form.get('total_items', 3) }}" required>
          </div>
          <div class="field">
            <label>Distinct items</label>
            <input type="number" step="1" name="num_distinct_items" value="{{ request.form.get('num_distinct_items', 3) }}" required>
          </div>
        </div>

        <div class="field-row">
          <div class="field">
            <label>Subtotal (cents)</label>
            <input type="number" step="1" name="subtotal" value="{{ request.form.get('subtotal', 2500) }}" required>
          </div>
          <div class="field">
            <label>Min item price</label>
            <input type="number" step="1" name="min_item_price" value="{{ request.form.get('min_item_price', 500) }}" required>
          </div>
        </div>

        <div class="field">
          <label>Max item price</label>
          <input type="number" step="1" name="max_item_price" value="{{ request.form.get('max_item_price', 1200) }}" required>
        </div>

        <div class="field-row">
          <div class="field">
            <label>Partners on shift</label>
            <input type="number" step="1" name="total_onshift_partners" value="{{ request.form.get('total_onshift_partners', 20) }}" required>
          </div>
          <div class="field">
            <label>Partners busy</label>
            <input type="number" step="1" name="total_busy_partners" value="{{ request.form.get('total_busy_partners', 12) }}" required>
          </div>
        </div>

        <div class="field">
          <label>Outstanding orders</label>
          <input type="number" step="1" name="total_outstanding_orders" value="{{ request.form.get('total_outstanding_orders', 15) }}" required>
        </div>

        <button type="submit">Predict delivery time</button>
      </form>
    </div>

    <div class="panel result-panel">
      <h2>Estimated arrival</h2>

      {% if error %}
        <div class="error">{{ error }}</div>
      {% endif %}

      {% if prediction is not none %}
        <div class="eta-value">{{ prediction }}</div>
        <div class="eta-unit">minutes</div>
        <div class="route-line"></div>
        <div class="placeholder">Predicted by the trained network<br>from current order &amp; fleet load.</div>
      {% else %}
        <div class="placeholder">Fill in the order details<br>and run a prediction to see<br>the estimated delivery time here.</div>
      {% endif %}
    </div>

  </div>

  <footer>Porter delivery-time estimator &middot; Keras regression model served via Flask</footer>
</div>
</body>
</html>


how to add this link to github for direct click website access